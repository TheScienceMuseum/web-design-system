import { render } from "sass";

export default {
  render: (args) =>
    `<span class="u-zip">Add class to get dynamic underline, or incorporate the Sass mixin. N.B. only works on inline elements.</span>`,
};

export const Underlines = {};
